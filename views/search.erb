<h3>Available Records</h3>

<% if @album.any? %>
  <ul>
    <% @album.sort_by(&:name).each do |album| %>
      <li><%= album.name %></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no records to display.</p>
<% end %>
